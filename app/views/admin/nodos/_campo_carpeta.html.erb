<% pdir = [] 
  Dir.chdir(Rails.application.config.x.heb412_ruta.join('NODO')) { 
  pdir = Dir.glob("./**/") } %>
<%= f.input :carpeta, 
  collection: pdir,
  input_html: {
    "data-toggle" => 'tooltip',
    "title" => 'Carpeta dentro del carpeta NODO de la nube, donde pondrá la información del nodo'
  } %>

