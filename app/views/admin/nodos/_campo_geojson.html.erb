<% pgeo = [] 
  Dir.chdir(Rails.application.config.x.heb412_ruta.join('Nodos')) { 
  pgeo = Dir.glob("**/*geojson") } %>
<%= f.input :geojson, 
  collection: pgeo,
  input_html: {
    "data-toggle" => 'tooltip',
    "title" => 'Ruta al GeoJSON del nodo'
  } %>

